# GitHub Launch Kit

This file is a ready-to-use checklist for polishing the public GitHub surface of `helix-agent`.

## Recommended Repository Description

Option A:

`Multi-provider MCP server for Claude Code with Ollama, Codex, and OpenAI-compatible agents.`

Option B:

`Claude Code-oriented MCP runtime for Ollama, Codex, and OpenAI-compatible models.`

Option C:

`One MCP server for Claude Code to route work across Ollama, Codex, and OpenAI-compatible models.`

## Recommended Website URL

Use the main repository URL unless you later publish docs elsewhere.

## Recommended Topics

- `mcp`
- `model-context-protocol`
- `claude-code`
- `agent`
- `agents`
- `ollama`
- `codex`
- `openai-compatible`
- `python`
- `developer-tools`

## Social Preview

Upload:

- `docs/assets/helix-agent-social-preview.svg`

If GitHub rejects SVG for social preview in the web UI, export it to PNG first and upload the PNG instead.

## Suggested Pinned README Sections

The strongest public flow is:

1. Short value statement
2. Architecture image
3. Why it is different
4. Quick examples
5. Setup

## Release Copy

Short version:

`helix-agent turns the original Ollama-focused helix-agent into a multi-provider MCP runtime for Claude Code, with Ollama, Codex, OpenAI-compatible APIs, and persistent background agents.`

Longer version:

`helix-agent is a multi-provider MCP server for Claude Code. It keeps the local Ollama workflow, adds Codex and OpenAI-compatible providers, and exposes a Claude Code-style background agent lifecycle so delegation feels consistent across providers.`
